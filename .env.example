# Telegram
BOT_TOKEN=8390833786:AAGx0LycQgOIdAsCP8Ebd2KAYk5Zx7d3qEY

# Access control (channel join check)
CHANNEL_ID=-1003334593795
CHANNEL_USERNAME=@PIMX_PASS
CHANNEL_LINK=https://t.me/PIMX_PASS

# Links
WEBSITE_URL=https://pimxpass.site/PIMX/PASS/BOT

# Data provider: "db" (internal scanner) or "api" (use remote backend)
DATA_PROVIDER=db
API_BASE_URL=https://your-backend.example/api

# Database
DB_PATH=./data/pimx_bot.db

# Scanner (only used when DATA_PROVIDER=db)
SERVERS_TO_TEST=1000
SCAN_INTERVAL_SECONDS=3600
MIN_SELECTED_SERVERS=100
MAX_SELECTED_SERVERS=150
SOURCE_FETCH_TIMEOUT_SECONDS=15

# Server test (only used when DATA_PROVIDER=db)
TEST_TIMEOUT_SECONDS=3
MAX_LATENCY_MS=500
MAX_CONCURRENCY=80
ACTIVE_CONFIRMATIONS=2
REAL_TEST_MODE=auto
XRAY_PATH=
TEST_TARGET_HOST=www.google.com
TEST_TARGET_PORT=80
TEST_TARGET_PATH=/generate_204
TEST_TARGET_MODE=auto
CORE_PATH_SEARCH_TIMEOUT_SECONDS=2.0
CORE_TEST_CONCURRENCY=3
CORE_STARTUP_TIMEOUT_SECONDS=1.5

# Bot UI
SERVERS_PER_PAGE=10
LIST_UPDATE_INTERVAL_SECONDS=3
SESSION_TTL_SECONDS=3600
MEMBERSHIP_CHECK_INTERVAL_SECONDS=120
READ_DB_POOL_SIZE=4

# Optional HTTP server (useful for hosting platforms / uptime monitors)
WEB_HOST=0.0.0.0
WEB_PORT=8080
# Optional HTTPS (provide full paths to cert and key to enable TLS)
WEB_SSL_CERT=
WEB_SSL_KEY=

# If set, long configs are copied as a link (1-click copy) instead of being sent in chat
# For Telegram mini-app, this MUST be an HTTPS URL on a public domain (no localhost / plain HTTP).
# If your app listens on :8080 internally, put it behind a reverse proxy (nginx/caddy) on :443 and set:
PUBLIC_BASE_URL=https://pimxpass.site
WEB_SKIP_TOP_SERVERS=0
